import "./../assets/result-search.scss";
const ResultSearch = () => {
  return (
    <div className="result-search">
      <ul>
        {/* {data &&
          data.map((i: { id: number; label: string; percent: number }) => {
            return (
              <li key={i.id} className="px-8 my-8">
                {i.label}
              </li>
            );
          })} */}
      </ul>
      {/* {loading && <Loading />} */}
    </div>
  );
};

export default ResultSearch;
